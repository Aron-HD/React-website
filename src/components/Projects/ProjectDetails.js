import { Fragment } from "react";
import useFetchFile from "../useFetchFile";

const ProjectDetails = () => {
  //   const { data: html, isPending, error } = useFetchFile();
  return (
    <Fragment>
      <h2>Project details</h2>
    </Fragment>
  );
};

export default ProjectDetails;
